import styled from "styled-components";

const SidebarOptions = ({ Icon, title }) => {
	return <SidebarOption></SidebarOption>;
};

const SidebarOption = styled.div``;

export default SidebarOptions;
